# Matchy
Matchy matches matchees

## Commands
### /match

## Dependencies
* `python3` obviously
* `discord.py` python module
* `pytest` for testing

## Token
Create a `config.py` file with a `TOKEN=<token>` entry
